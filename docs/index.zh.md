# Heta

Heta 是面向知识库构建的 Python framework，发布包名为 `heta`。

它把 HetaDB 的核心能力拆成可组合组件：

- `common`：模型、对象存储、向量存储、SQL 存储等基础组件。
- `kb`：parser、step、procedure、recipe 和 knowledge base 构建 API。

从 [Quick Start](quick-start.zh.md) 开始，可以用一个本地文本文件跑通第一个知识库构建。
